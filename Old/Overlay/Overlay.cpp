#include "Overlay.h"

using namespace std;

Overlay::Overlay()
{
    //ctor
}

Overlay::~Overlay()
{
    //dtor
}

bool Overlay::leftClicked(sf::Vector2f mouseCoord)
{
    //loop over our panels
    return true;

    return false;//we must not have had the mouse
}
bool Overlay::rightClicked(sf::Vector2f mouseCoord)
{
    //loop over our panels
    return true;

    return false;//we must not have had the mouse
}
bool Overlay::middleClicked(sf::Vector2f mouseCoord)
{
    //loop over our panels
    return true;

    return false;//we must not have had the mouse
}
bool Overlay::mouseOver(sf::Vector2f mouseCoord)
{
    //loop over our panels
    return true;

    return false;//we must not have had the mouse
}
