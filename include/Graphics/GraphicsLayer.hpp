#ifndef GRAPHICSLAYER_H
#define GRAPHICSLAYER_H

enum class GraphicsLayer
{
    BackgroundFar = 0,
    BackgroundClose,
    GModules,
    ShipHull,
    ShipAppendagesLower,
    ShipAppendagesUpper,
    Projectiles,
    HUD,
    Menus,
    maxLayers,
};

#endif // GRAPHICSLAYER_H
